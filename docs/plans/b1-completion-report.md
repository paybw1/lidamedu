# B1 완료 보고 — 학습시간 누적 버그 수정 및 데이터 정정

> 완료일: 2026-08-13 · Stage 1 코드 `bb5109f8`(배포됨) · Stage 2 정정 SQL `scripts/sql/20260813_b1_time_correction.sql`(적용 완료)
> 원본 보존: 정정·롤백 SQL 에 215건 (attempt_id, old_ms, new_ms) **인라인 기록** — 파일 자체가 원본 대장

## 1. Stage 2 진행 판단 (V5 표본 미충족 상태 — 근거 명시)

배포(8/13 11:19) 이후 신규 pack 세션 **0건** — §2의 "5건 확보" 미충족, "1주 경과" 도 미도래. 그러나 **§3-1(Phase 3 학생 확대 전 완료·전윤서 2주차 동반 작성 가능성)이 시간상 우선**하고, §2의 대기 무익 논거(표본이 안 모임 = 유입도 느림)가 현 시점에도 성립하며, 정정은 attempt_id 단위로 스코프되어 신규 데이터와 무간섭 + 코드 수정은 단위 테스트 C1~C4 로 이미 보증되므로 진행했다. **V5 는 지속 관찰 항목으로 남긴다**: `node tmp/b1-diagnosis/stage0-refined.mjs` 재실행 시 "누적 확정 0" 유지 확인(신규 pack 세션 5건 도달 시 1회).

## 2. 정정 실행 내역

- 대상: 누적 확정 **22세션 · attempt 215건** (전건 2026-06-01 ~ 07-17 응답분)
- 방식: 승인된 **값 오름차순 차분** — 생성기(`tmp/b1-diagnosis/gen-correction.mjs`)가 Stage 0 3중 판정을 재수행해 대상을 재확정 후 SQL 산출. 생성 시점 검증: 전 세션 차분합=max·음수 0
- 안전장치: 단일 트랜잭션 + **사전 assert**(현재 값=old_ms 전건 일치 — 이중 실행·경합 차단) + **사후 assert**(적용 215/215 아니면 예외로 전체 롤백)
- 절차: 정정 dry-run(begin…rollback) 통과 → 적용 → 롤백 dry-run(V3) 통과 → 정정 상태 유지 확인

## 3. 검증 결과

| # | 검증 | 결과 |
|---|---|---|
| B1-V1 | 정정 후 누적 시그니처 검출 | ✅ **누적 확정 0건** (시그니처 매치 50건은 전부 기지 오탐군 — 정상 문항별 24·균등분배 6 등) |
| B1-V2 | 세션별 정정 합계 = 정정 전 max | ✅ **22/22** |
| B1-V3 | 롤백 SQL dry-run | ✅ 트랜잭션 내 원본 복원 + assert 통과 후 rollback(무변경). 실제 롤백 = `node scripts/run-prod-sql.mjs scripts/sql/20260813_b1_time_correction_rollback.sql` 한 줄 |
| B1-V4 | at-risk·백분위 판정 변화 | **변화 없음 — 구조적 사유**: at-risk 의 학습시간·정답률 신호는 최근 7·14일 추세 윈도우인데 오염 데이터는 전건 27일 이전(≤7/17)이라 이미 윈도우 밖. 코호트 브래킷은 표본 부족(8<10)으로 잠김. **정정의 실효는 누적 지표**(학습현황 주별 히스토리·합격자 분석·향후 백분위 모수)의 정확화이며, "가려진 신호"는 현행 추세 신호가 아니라 이 누적 축이었다 |
| B1-V5 | 신규 데이터 누적 미발생 | ⏳ 표본 0건 — §1 대로 지속 관찰(5건 도달 시 재스캔) |

## 4. 정정 전후 KPI (상담자 참조용 ★)

**전체**: 114.4h → **100.8h** (−13.7h, −11.9%)

| 학생 | 정정 전 | 정정 후 | 변화 | 상담 참고 |
|---|---|---|---|---|
| **Jiyung PARK** | 11.29h | **2.01h** | −82% | 학습시간이 약 1/5 로 — 질문 시 "학습시간 집계 오류를 바로잡았다"로 즉답 |
| **전윤서** ★운영 게이트 코호트 | 5.38h | **1.33h** | −75% | 2주차 동반 작성 **이전에 정정 완료**(§3-1 충족). 동일 즉답 |
| ggheroes | 0.64h | 0.38h | −41% | 〃 |
| (관리자 계정 3건) | — | — | 합계 −0.08h | 표시 무의미 |

- 고지: 채택 방침대로 **선제 공지 없음**. 위 표가 상담자 인지용 자료다.
- Phase 3 관찰 조건 보호: 전윤서 학생이 계획 기능을 시작하기 전에 정정이 끝났으므로, 새 화면에서 보는 값이 처음부터 정정값이다.

## 5. 산출물·변경 파일

- `scripts/sql/20260813_b1_time_correction.sql` / `..._rollback.sql` — 원본 215건 인라인 보존 + assert (적용 완료 / 롤백 대기)
- Stage 1(기배포): `mcq-pack-sheet.tsx` 문항별 시간 + `study-volume.ts` `computePerQuestionTimeMs`·`PER_QUESTION_TIME_CAP_MS`(30분) + 단위 테스트 4건
- 진단·검증 도구(재실행 가능): `tmp/b1-diagnosis/` — stage0-refined(시그니처 재스캔=V5 관찰), gen-correction(생성기), verify-v2, before/after 스냅샷

*B1 종료. B2(크론) 등 다른 별건 트랙으로 자동 진행하지 않는다.*
