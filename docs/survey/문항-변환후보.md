# S6 "문제"→"문항" 변환 후보 (검토용 — 적용 0)

> 규칙: **카운터·단위로 쓰인 "문제"만 "문항"으로**. 개념·nav("다음 문제·문제 목록·이 문제·문제 풀이·문제 중심")는 **유지**.
> 범위 = 학생 대면 렌더만(admin·landing(home/)·코드주석 제외). "문항"은 이미 대부분 표준(quiz-setup·quiz-result·mcq-pack-* 카운터는 이미 "문항") — 본 후보는 잔여 "문제" 카운터.
> ★사용자 검토: 아래 **A 변환** 확정 + **B 경계**(카운터/개념) 판정.

---

## A. 변환 (카운터·단위 "문제" → "문항") — 명확

| 위치 | 현재 → 변경 |
|---|---|
| `subjects/screens/science/quiz-setup.tsx:171` | "총 등록된 **문제**: {n}" → "총 등록된 **문항**: {n}" |
| `subjects/screens/science/quiz-setup.tsx:210` | "{s.problemCount}**문제**" → "{…}**문항**" |
| `subjects/screens/science/quiz-setup.tsx:276` | "{n}**문제**" → "{n}**문항**" |
| `subjects/components/science-hub.tsx:146` | "총 **문제**" → "총 **문항**" |
| `subjects/components/science-hub.tsx:362` | "총 40**문제** 중 과목별 10**문제**씩" → "40**문항** 중 … 10**문항**씩" |
| `subjects/components/subject-learning-hub.tsx:142` | "오답 {n}**문제** 다시 풀기" → "오답 {n}**문항** 다시 풀기" |
| `subjects/components/node-mini-graph.tsx:116` | "조문 N · 판례 N · **문제** {n}" → "… **문항** {n}" (그래프 카운트) |
| `latest/screens/mcq-pack-ox-exam.tsx:295,296` | "{packCount}개 **문제**에서 추출" → "…개 **문항**에서 추출" (정오문제 지문 = 그대로) |
| `dashboard/components/dash-kpi-strip-v2.tsx:30` | KPI "푼 **문제** 수" → "푼 **문항** 수" |
| `dashboard/components/dash-header.tsx:167` | KPI "푼 **문제** 수" → "푼 **문항** 수" |
| `exam-results/screens/passer-cases-components.tsx:58` | "{n}**문제**" → "{n}**문항**" (합격자 분석 카운트) |

→ A ≈ 12곳/8파일.

---

## B. 경계 — ★카운터인지 개념인지 판정 요청

| 위치 | 현재 | 쟁점 | 제 의견 |
|---|---|---|---|
| `latest/screens/mcq-pack-sheet.tsx:327` | "다시 볼 **문제** {flagged.size}" | 플래그한 문제 수 배지(카운터) vs "다시 볼 문제"(개념) | **카운터 → 문항** 약권장 |
| `study/screens/srs.tsx:216` | "지금 풀어야 할 **문제** {n}건" | "문제"=개념 + "건"=카운터 단위. 문제→유지하고 "건"을 손볼지? | **유지**(문제=개념, "건"은 별도 카운터단위 — 차후) |
| `study/screens/wrong-note-print.tsx:148` | "**문제** {ordinal}" | 오답노트 인쇄물 문제 머리표(식별 라벨, "문제 1"·"문제 2") | **유지**(서수 라벨, "이 문제"류 식별) or 문항 — 판정 요청 |
| `study/components/passer-calibration-card.tsx:74` | KPI "총 **문제** 풀이" | "문제 풀이"=개념(act) | **유지**(개념 "문제 풀이") |
| (`subjects/screens/problem-viewer` 등 "문제 N번" 머리표가 있으면 동일 범주) | "**문제** {n}번" | 문제번호 식별 라벨 | wrong-note-print과 동일 판정 적용 |

---

## C. 유지 (개념·nav — 변경 안 함)

- "객관식 **문제** 중심"(science-hub:138 등), "다음/이전 **문제**", "**문제** 목록", "이 **문제**", "**문제** 풀이", 3탭의 "조문·판례·**문제**".
- `srs.tsx:382` "총 정오**문제** 항목" = "정오문제"(OX 포맷 라벨) + "항목"(단위) — "문제" 카운터 아님 → 유지.
- 이미 "문항" 표준인 곳(quiz-setup·quiz-result·mcq-pack-result·mcq-pack-sheet 카운터, GS "문항") — 변경 없음.

---

## 적용 / 검증
- 검토(A 확정 + B 판정) 후 적용. **단일 개념(문항) 커밋.** §3 `git diff`로 식별자(`problemCount`·`problemNumber`·`.from(`·enum) 변경 0 검증(전부 표시 문자열만). typecheck.
- 규모: A ≈12 + B 채택분. 소규모. → 표준어 본적용(S1~S6) 일단락.
