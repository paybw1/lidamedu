# S5 "회차" 3분할 분류표 (검수 확정 — 적용)

> "회차" 동음이의 분류 → 사용자 검수 확정(2026-06-24). 의미별 적용.
> 표준(확정): **공식 시험회차 = "회차"(유지)** / **내 N번째 응시 = "응시"**(★검수서 "시도"→"응시"로 수정 — 시험은 "응시"가 격에 맞고 입력허브·OX "시험"과 일관) / **퀴즈 풀이 세션 = "풀이" 또는 맥락 재서술**.
> 범위 = 학생 대면 렌더 문자열만. DB `examRoundNo`·`roundNumber`·`quiz_session`·`sessionId` 식별자 불변.

---

## A. 공식 시험회차 → "회차" **유지 (변경 없음)**

| 위치 | 의미 |
|---|---|
| `gs.tsx:103,210,211` · `gs-take.tsx:64` · `gs-result.tsx:62` · `gs-my-series.tsx:75,111,118,120,131` · `gs-peer-review-round.tsx:76` · `gs-points.tsx:27` · `gs-distinguished.tsx:198,210,307` | GS 공개 회차(강사 개설·공개) |
| `cases/case-training-take.tsx:223,224,269` · `case-training-conclusion.tsx:157,160,161` | "GS 회차" 연결 |
| `subjects/components/tabs/problems-tab.tsx:561` | sort 컬럼 "연도/회차"(기출 공식회차) |
| `latest/mcq.tsx:665` | pack 추가 폼 "회차"(staff, 공식회차 입력) |

★**#4 재확인**: `gs-result.tsx:62`("아직 응시하지 않은 회차")·`gs-take.tsx:64`("이 회차는 아직 공개되지 않았습니다") = **GS 라운드(제출 안 함/공개 전) 참조 = 공식**, "내 N번째 응시" 뉘앙스 아님 → A 유지 확정.

---

## B. 내 N번째 응시 → **"응시"** (확정, "시도" 아님)

| 위치 | 현재 → 변경 |
|---|---|
| `my-ox-session-result.tsx:21` | "정오문제 **회차** 결과" → "정오문제 **응시** 결과" |
| `my-ox-session-result.tsx:143` | "이 **회차**의 지문을…" → "이 **응시**의 지문을…" |
| `my-ox-sessions.tsx:147` | 정렬 "**회차**순" → "**응시**순" |
| `mcq-pack-ox-exam.tsx:295` | "**회차** 이력에는…" → "**응시** 이력에는…" |
| `mcq-pack-result.tsx:279` | "이번 **회차** 오답만 모아 새 **회차**로." → "이번 **응시** 오답만 모아 새 **응시**로." |
| `mcq-pack-result.tsx:586` | "지난 **회차** ±" → "지난 **응시** ±" |
| `study/api/session-from-attempts.tsx:81` | "이 **회차**에 오답이 없습니다." → "이 **응시**에 오답이 없습니다." |
| `study/api/session-from-attempts.tsx:103` | originLabel "이번 **회차** 오답" → "이번 **응시** 오답" |

---

## C. 퀴즈 풀이 세션 → **"풀이" / 맥락 재서술** (확정)

| 위치 | 현재 → 변경 | 방식 |
|---|---|---|
| `subjects/problem-viewer.tsx:318,320,321` | "오답노트/즐겨찾기/퀴즈 **회차**" → "오답노트/즐겨찾기/퀴즈 **풀이**" | 치환 |
| `subjects/quiz-setup.tsx:243` | eyebrow "학습 **회차**" → "**풀이 설정**" | ★재서술("학습 풀이"=어색) |
| `subjects/science/quiz-setup.tsx:170` | "퀴즈 **회차**를 생성합니다" → "**문제 풀이를 시작합니다**" | ★재서술 |
| `subjects/science/quiz-setup.tsx:349` | "**회차** 생성까지 동작합니다" → "**풀이 시작까지** 동작합니다" | ★재서술 (※로드맵 노트 노출은 별개 정리 대상) |
| `study/bookmarks.tsx:216` | "한 **회차**로 묶어 풀기" → "**묶어 한 번에 풀기**" | ★재서술(#3 확정) |
| `study/components/study-aids-list.tsx:471` | "건을 한 **회차**로 묶어 풀기" → "건을 **묶어 한 번에 풀기**" | ★재서술 |

---

## 적용 / 검증
- **B(응시) 커밋 / C(풀이·재서술) 커밋 분리.** 각 §3 `git diff`로 식별자(`examRoundNo`·`roundNumber`·`quiz_session`·`sessionId`·`.from(`) 변경 0 검증. typecheck.
- 규모: A 유지 0 · B 응시 8곳/4파일 · C 풀이 8곳/4파일(4곳 재서술). 총 ~16 렌더 변경.
